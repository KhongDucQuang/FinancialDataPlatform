executor: "LocalExecutor"

webserver:
  service:
    type: NodePort
    ports:
      - name: airflow-ui
        port: 8080
        nodePort: 30080
redis:
  enabled: false
flower:
  enabled: false

postgresql:
  enabled: true

dags:
  gitSync:
    enabled: true